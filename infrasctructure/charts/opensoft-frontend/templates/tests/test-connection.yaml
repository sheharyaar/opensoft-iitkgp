apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "opensoft-frontend.fullname" . }}-test-connection"
  labels:
    {{- include "opensoft-frontend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "opensoft-frontend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
